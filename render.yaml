services:
  - type: web
    name: virtual-online-study-api
    env: node
    plan: free
    buildCommand: |
      npm install
      cd client && npm install && npm run build && cd ..
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
